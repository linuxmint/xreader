version: 2.0

jobs:

  "mint-18":
    docker:
      - image: linuxmintd/linuxmint-sylvia
    steps:
      - run: mint-build xapps -i
      - run: mint-build xreader -i

workflows:
  version: 2
  build:
    jobs:
      - "mint-18"
